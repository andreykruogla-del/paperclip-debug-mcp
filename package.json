{
  "name": "paperclip-triage",
  "version": "0.1.0",
  "private": true,
  "description": "MCP-first incident triage layer for Paperclip agent systems",
  "type": "module",
  "scripts": {
    "build": "npx tsc -p tsconfig.json",
    "mcp:stdio": "tsx scripts/mcp-stdio.ts",
    "check": "npx tsc -p tsconfig.json --noEmit"
  },
  "keywords": [
    "paperclip",
    "mcp",
    "triage",
    "incident"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.67"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
