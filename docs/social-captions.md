# Social Captions

Use these with:

- `terminal-showcase.mp4` for X and LinkedIn
- `terminal-loop.gif` for GitHub README and short social snippets

## X (short)

```text
Debugging Paperclip should not mean jumping between logs, issues, comments, runs, and handoff traces.

Built a terminal-first flow with Paperclip Debug MCP:
one MCP surface for triage, trace_handoff, and incident packet generation.

Looking for a few early testers:
https://github.com/andreykruogla-del/paperclip-debug-mcp
```

## X (with stronger hook)

```text
Most incident time is lost before root-cause, not during the fix.

This terminal demo shows how we go from fragmented signals to one run-aware investigation flow with Paperclip Debug MCP.

Early testers open:
https://github.com/andreykruogla-del/paperclip-debug-mcp
```

## LinkedIn

```text
One thing we kept seeing in Paperclip debugging:
the data exists, but the investigation flow is fragmented.

Runs, issue threads, comments, logs, and handoff traces often live in different places, so teams spend too much time rebuilding context manually.

We put together a terminal-first workflow in Paperclip Debug MCP to make this path more structured:
- prioritize incidents
- trace run-aware handoffs
- build handoff evidence packets faster

If this pain sounds familiar, we are looking for a few early testers and design partners:
https://github.com/andreykruogla-del/paperclip-debug-mcp
```
